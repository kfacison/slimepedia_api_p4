// import express,{ Router } from "express";
// import * as foodsCtrl from "../../controllers/api/foods.ts";
// import { Request, Response } from "express";

// const router: Router = express.Router();

// //ROUTES FOR SLIMES
// // GET /api/foods/:id -- Gets food info
// router.get("/:id", foodsCtrl.getFood);

// // GET /api/foods -- Gets all foods
// router.get("/", foodsCtrl.getAll);

// // POST /api/foods/:id -- Create new food
// router.post("/:id", foodsCtrl.createFood);

// // PUT /api/foods/:id -- Update food
// router.put("/:id",foodsCtrl.update);

// // DELETE /api/foods/:id -- Delete food
// router.delete("/:id", foodsCtrl.deleteFood);

// export default router;